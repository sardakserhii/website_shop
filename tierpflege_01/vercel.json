{
    "buildCommand": "npm ci --no-audit --no-fund && npm run build",
    "outputDirectory": "dist",
    "framework": "vite",
    "ignoreCommand": "git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- tierpflege_01 && exit 0 || exit 1"
}
